services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NUXT_MONGODB_URI=${MONGODB_URI}
      - NUXT_CLOUDINARY_URL=${CLOUDINARY_URL}
      - NUXT_ROBOFLOW_API_KEY=${ROBOFLOW_API_KEY}
    env_file:
      - .env
    restart: unless-stopped